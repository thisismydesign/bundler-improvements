# `authors` and `email` fields should be edited

Gem::Specification.new do |spec|
  spec.authors       = ["thisismydesign"]
  spec.email         = ["thisismydesign@users.noreply.github.com"]

  spec.add_development_dependency "coveralls"
end
